/*
  This table serves as the test of the root to run in order to run the rest of the transforms in the pipeline of the DWH.

  It doesn't need to actually do anything, just run and be referenceable as a managed table in Mozart.

*/
select 1 as one